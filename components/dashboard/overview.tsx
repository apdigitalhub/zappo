import { ArrowUpRight, Download, Filter } from "lucide-react";
import { AnalyticsPreview } from "@/components/charts/analytics-preview";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { kpis, transactions } from "@/lib/demo-data";
export function Overview() { return <div className="space-y-6"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-center"><div><p className="text-sm text-muted-foreground">Dashboard / Overview</p><h1 className="text-4xl font-semibold tracking-tight">Revenue command center</h1></div><div className="flex gap-2"><Button variant="outline"><Filter className="size-4" /> Date range</Button><Button><Download className="size-4" /> Export</Button></div></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{kpis.map((k) => <Card key={k.label}><CardContent className="p-5"><p className="text-sm text-muted-foreground">{k.label}</p><div className="mt-3 flex items-end justify-between"><p className="text-3xl font-semibold">{k.value}</p><Badge variant="success">{k.change}</Badge></div></CardContent></Card>)}</div><AnalyticsPreview /><Card><CardHeader><CardTitle>Recent transactions</CardTitle></CardHeader><CardContent><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="text-left text-muted-foreground"><tr><th className="py-3">ID</th><th>Customer</th><th>Amount</th><th>Status</th><th>Gateway</th><th></th></tr></thead><tbody>{transactions.map((t) => <tr key={t.id} className="border-t"><td className="py-4 font-mono">{t.id}</td><td>{t.customer}</td><td>{t.amount}</td><td><Badge variant={t.status === "Successful" ? "success" : t.status === "Failed" ? "danger" : "warning"}>{t.status}</Badge></td><td>{t.gateway}</td><td><ArrowUpRight className="size-4" /></td></tr>)}</tbody></table></div></CardContent></Card></div>; }
